import { style } from '@vanilla-extract/css';
import { rem } from "@mantine/core";

export const metricsSelector = style({
    display: 'block',
    marginTop: rem(8),
    marginBottom: rem(8),
    width: '100%',
});